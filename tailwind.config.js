/** @type {import("@types/tailwindcss/tailwind-config").TailwindConfig } */
const config = {
  content: ["./src/*/*.{tsx,jsx}"],
  theme: {
    extend: {},
  },
  plugins: [],
};

module.exports = config;
